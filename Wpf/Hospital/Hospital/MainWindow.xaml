<Window x:Class="YourNamespace.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Patients Management" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- DataGrid za prikaz liste pacijenata -->
        <DataGrid x:Name="PatientsDataGrid" Grid.Row="0" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" />
                <DataGridTextColumn Header="Full Name" Binding="{Binding FullName}" />
                <DataGridTextColumn Header="Date of Birth" Binding="{Binding DateOfBirth}" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Gumb za dodavanje novog pacijenta -->
        <Button Grid.Row="1" Content="Add New Patient" Click="AddPatientButton_Click" HorizontalAlignment="Center" />
    </Grid>
</Window>
